import styled from 'styled-components';

import { FlexRow } from 'components';

export const Container = styled.div`
  height: 100%;
  width: 100%;
`;

export const LoaderWrapper = styled(FlexRow)`
  margin: 20px;
`;
